---
title: Magica Backend
description: API and durable agent runtime for Magica chat
---

# Magica Backend

API and durable agent runtime for Magica chat. **Postgres** is the source of truth; **Trigger.dev** runs the agent loop and projects Realtime streams to clients.

## What this docs site covers

- Local setup and environment configuration
- Authentication with Clerk
- How agent runs, credits, and media uploads work
- HTTP API reference for chats, messages, runs, tools, credits, and attachments

## Stack at a glance

| Layer | Role |
| --- | --- |
| `src/app/api/v1` | HTTP: chats, messages, runs, tools, credits, attachments |
| `src/app/api/webhooks` | Magica (Svix) + Transloadit + Clerk |
| `src/services` | Transactions, dispatch, progressive credits, media |
| `src/agent` | Context restore, turn loop, finalize |
| `src/trigger` | `agent-run`, `magica-poll`, reconcile sweeper, Realtime |

## Next steps

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Install dependencies, configure env, run the API and Trigger worker.
  </Card>
  <Card title="API overview" icon="code" href="/api/overview">
    Browse authenticated v1 endpoints and webhooks.
  </Card>
</CardGroup>
